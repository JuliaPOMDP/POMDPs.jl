# language: julia
# os:
#   - linux
#   - osx
# julia:
#   - 1.0
# notifications:
#   email:
#     - sunbergzach@gmail.com
# before_script:
#   - export PATH=$HOME/.local/bin:$PATH
# script:
#   - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#   - julia --check-bounds=yes -e 'using Pkg; Pkg.clone(pwd()); Pkg.test("POMDPs")'
# after_success:
#   - julia -e 'using Pkg; Pkg.add("Documenter"); cd(Pkg.dir("POMDPs")); include(joinpath("docs", "make.jl"))'

language: julia

julia:
  - 1.0
  - nightly

os:
  - linux
  - osx

notifications:
  email:
    - sunbergzach@gmail.com

script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia --project --check-bounds=yes -e 'import Pkg; Pkg.build(); Pkg.test(; coverage=true)'

after_success:
  - julia --project -e 'import Pkg; Pkg.add("Documenter"); include("docs/make.jl")'
  - julia --project -e 'import Pkg; Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
